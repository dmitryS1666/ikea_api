# 🔑 Переменные окружения

## Все ключи уже прописаны в docker-compose.yml

### Обязательные переменные (уже настроены):

✅ **DB_HOST** = postgres  
✅ **DB_USERNAME** = postgres  
✅ **DB_PASSWORD** = postgres  
✅ **DB_PORT** = 5432  
✅ **REDIS_URL** = redis://redis:6379/0  
✅ **MONGODB_URI** = mongodb://mongodb:27017/ikea  
✅ **RAILS_ENV** = development  

### Переменные, которые можно настроить через .env:

📝 **JWT_SECRET** - секрет для JWT токенов (по умолчанию: change_me_to_secret_key_please)  
📝 **REDIS_PASSWORD** - пароль Redis (опционально, по умолчанию: пусто)  
📝 **RAILS_MAX_THREADS** - количество потоков (по умолчанию: 5)  

## Создание .env файла (опционально)

```bash
# Генерация JWT_SECRET
JWT_SECRET=$(ruby -e "require 'securerandom'; puts SecureRandom.hex(64)")

# Создание .env
cat > .env << EOL
JWT_SECRET=$JWT_SECRET
EOL
```

**Примечание**: Все ключи уже прописаны в docker-compose.yml и будут работать без .env файла.  
Создайте .env только если хотите изменить значения по умолчанию.
