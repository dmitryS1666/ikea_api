# 🌐 Настройка домена для Kamal

## 📋 Обзор

После успешного деплоя через Kamal, вы можете привязать домен к вашему приложению. Kamal поддерживает автоматическую настройку SSL через Let's Encrypt.

---

## 🔧 Шаг 1: Настройка DNS

### 1.1. Добавьте A-запись в DNS вашего домена

Перейдите в панель управления вашего DNS-провайдера (например, Cloudflare, Namecheap, GoDaddy) и добавьте:

```
Тип: A
Имя: @ (или ваш поддомен, например: api)
Значение: 45.135.234.22
TTL: 3600 (или Auto)
```

**Примеры:**
- Для `example.com` → A запись `@` → `45.135.234.22`
- Для `api.example.com` → A запись `api` → `45.135.234.22`

### 1.2. Дождитесь распространения DNS

Обычно занимает от нескольких минут до 24 часов. Проверить можно командой:

```bash
dig example.com +short
# Должен вернуть: 45.135.234.22
```

---

## 🔧 Шаг 2: Обновление конфигурации Kamal

### 2.1. Обновите `config/deploy.yml`

Добавьте секцию `proxy` для настройки домена и SSL:

```yaml
# Серверы для деплоя
servers:
  web:
    hosts:
      - 45.135.234.22
    # Настройка proxy для домена
    proxy:
      tls:
        # Автоматическая настройка SSL через Let's Encrypt
        certs: auto
      # Домены для вашего приложения
      hostnames:
        - example.com
        - www.example.com
      # Опционально: редирект с www на основной домен
      redirect: www.example.com
```

**Пример полной конфигурации:**

```yaml
servers:
  web:
    hosts:
      - 45.135.234.22
    proxy:
      tls:
        certs: auto
      hostnames:
        - api.example.com
```

### 2.2. Включите force_ssl обратно (опционально)

После настройки SSL, вы можете включить `force_ssl` обратно:

```ruby
# config/environments/production.rb
config.force_ssl = true
```

---

## 🔧 Шаг 3: Деплой с доменом

### 3.1. Закоммитьте изменения

```bash
git add config/deploy.yml config/environments/production.rb
git commit -m "Add domain configuration"
git push
```

### 3.2. Запустите деплой

```bash
kamal deploy
```

Kamal автоматически:
- Настроит SSL сертификаты через Let's Encrypt
- Настроит kamal-proxy для работы с доменом
- Настроит редиректы (если указаны)

---

## 🔒 Шаг 4: Проверка SSL

После деплоя проверьте:

```bash
# Проверка SSL сертификата
curl -I https://example.com/up

# Или через браузер
# Откройте https://example.com/api-docs
```

---

## 📝 Альтернативный вариант: Ручная настройка SSL

Если вы хотите настроить SSL вручную (например, через Nginx):

### 4.1. Установите Certbot

```bash
ssh deploy@45.135.234.22
sudo apt-get update
sudo apt-get install certbot python3-certbot-nginx
```

### 4.2. Получите сертификат

```bash
sudo certbot --nginx -d example.com -d www.example.com
```

### 4.3. Настройте Nginx

Обновите `/etc/nginx/sites-available/ikea_api` для работы с SSL.

---

## 🔄 Обновление конфигурации после привязки домена

### Если используете kamal-proxy:

1. Обновите `config/deploy.yml` (добавьте `proxy.hostnames`)
2. Запустите `kamal deploy`
3. Kamal автоматически настроит все необходимое

### Если используете Nginx:

1. Обновите конфигурацию Nginx
2. Получите SSL сертификат через Certbot
3. Перезапустите Nginx: `sudo systemctl restart nginx`

---

## 🎯 Рекомендации

1. **Используйте kamal-proxy для автоматической настройки SSL** - это проще и надежнее
2. **Добавьте оба домена** (с www и без) для полной поддержки
3. **Включите force_ssl** после настройки SSL для безопасности
4. **Проверьте DNS** перед деплоем, чтобы избежать проблем

---

## 📚 Дополнительная информация

- [Kamal Documentation - Proxy](https://kamal-deploy.org/docs/deploy/proxy)
- [Kamal Documentation - SSL/TLS](https://kamal-deploy.org/docs/deploy/ssl)

---

## ⚠️ Важные замечания

1. **DNS должен быть настроен ДО деплоя** - иначе Let's Encrypt не сможет выдать сертификат
2. **kamal-proxy должен быть запущен** - он управляет SSL сертификатами
3. **Порт 80 и 443 должны быть открыты** - для HTTP и HTTPS соответственно

