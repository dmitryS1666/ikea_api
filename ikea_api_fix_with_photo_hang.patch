diff --git a/app/services/product_reviews/index_service.rb b/app/services/product_reviews/index_service.rb
index 71825bc..73bf389 100644
--- a/app/services/product_reviews/index_service.rb
+++ b/app/services/product_reviews/index_service.rb
@@ -115,7 +115,10 @@ module ProductReviews
     end
 
     def only_with_photos(scope)
-      scope.where(id: photo_review_ids)
+      review_ids = photo_review_ids
+      return scope.none if review_ids.empty?
+
+      scope.where(id: review_ids)
     end
 
     def photo_review_ids
@@ -131,13 +134,9 @@ module ProductReviews
           'image/%',
           *PHOTO_FILENAME_SUFFIXES.map { |suffix| "%#{suffix}" }
         )
-        .select(photo_record_id_select_sql)
-    end
-
-    def photo_record_id_select_sql
-      return Arel.sql('active_storage_attachments.record_id') unless Review.columns_hash['id']&.type == :integer
-
-      Arel.sql('CAST(active_storage_attachments.record_id AS bigint)')
+        .distinct
+        .pluck(:record_id)
+        .filter_map { |record_id| Integer(record_id, exception: false) }
     end
 
     def with_photo_filter_enabled?
