# 🎨 Настройка React фронтенда в контейнере с Rails

## 📋 Обзор

React приложение собирается в статику и размещается в `public/` Rails. Rails обслуживает все: API, Admin, Swagger и Frontend.

## ✅ Преимущества

- ✅ Один контейнер для всего
- ✅ Нет необходимости в Nginx на сервере
- ✅ `kamal-proxy` работает напрямую на порту 80
- ✅ Простая архитектура
- ✅ Нет пробросов портов и танцев с бубнами

## 🔧 Шаг 1: Сборка React приложения

```bash
# Перейти в директорию React приложения
cd /path/to/your/react-app

# Установить зависимости (если нужно)
npm install

# Собрать приложение в статику
npm run build
# или
yarn build

# Результат будет в директории build/ (или dist/)
```

## 🔧 Шаг 2: Копирование в Rails

### Вариант A: Ручное копирование (для тестирования)

```bash
# Скопировать собранные файлы в public/ Rails
cp -r /path/to/react-app/build/* /path/to/rails-app/public/

# Убедиться, что index.html существует
ls -la /path/to/rails-app/public/index.html
```

### Вариант B: Автоматизация через скрипт

Создайте скрипт `scripts/build_frontend.sh`:

```bash
#!/bin/bash
set -e

REACT_APP_DIR="/path/to/your/react-app"
RAILS_PUBLIC_DIR="$(pwd)/public"

echo "🔨 Сборка React приложения..."
cd "$REACT_APP_DIR"
npm run build

echo "📦 Копирование в Rails public/..."
rm -rf "$RAILS_PUBLIC_DIR"/*
cp -r build/* "$RAILS_PUBLIC_DIR"/

echo "✅ Готово! Фронтенд скопирован в public/"
```

## 🔧 Шаг 3: Обновление Dockerfile

Если React приложение находится в отдельном репозитории/директории, добавьте в `Dockerfile`:

```dockerfile
# После COPY . .
# Если React приложение в той же директории:
COPY frontend/build /rails/public

# Или если React приложение в отдельном репозитории,
# соберите его перед docker build и скопируйте в public/
```

## 🔧 Шаг 4: Настройка API endpoints в React

В React приложении настройте базовый URL для API:

```javascript
// src/config/api.js
const API_BASE_URL = process.env.REACT_APP_API_URL || '/api/v1';

export default API_BASE_URL;
```

Или используйте относительные пути:

```javascript
// Все запросы к API будут идти через тот же домен
fetch('/api/v1/products')
```

## 🔧 Шаг 5: Деплой

```bash
# 1. Собрать React приложение
cd /path/to/react-app && npm run build

# 2. Скопировать в Rails public/
cp -r build/* /path/to/rails-app/public/

# 3. Деплой через Kamal
cd /path/to/rails-app
kamal deploy
```

## 📋 Структура после сборки

```
rails-app/
├── public/
│   ├── index.html          # Главный файл React SPA
│   ├── static/
│   │   ├── css/
│   │   ├── js/
│   │   └── media/
│   └── ...
├── app/
├── config/
└── ...
```

## 🎯 Маршрутизация

Rails routes обрабатывает запросы в следующем порядке:

1. `/api/*` → API контроллеры
2. `/admin/*` → Trestle Admin Panel
3. `/api-docs/*` → Swagger UI
4. `/up` → Health check
5. `/*` → FrontendController → `public/index.html` (React Router)

## ⚠️ Важные моменты

1. **React Router**: Используйте `BrowserRouter` с `basename` если нужно:

```javascript
import { BrowserRouter } from 'react-router-dom';

<BrowserRouter basename="/">
  <App />
</BrowserRouter>
```

2. **API запросы**: Используйте относительные пути или настройте proxy в `package.json` для разработки:

```json
{
  "proxy": "http://localhost:3000"
}
```

3. **Переменные окружения**: Используйте `REACT_APP_*` префикс:

```javascript
// .env
REACT_APP_API_URL=/api/v1

// В коде
const apiUrl = process.env.REACT_APP_API_URL;
```

## 🔄 CI/CD процесс

```bash
# 1. Собрать React
cd frontend && npm run build

# 2. Скопировать в Rails
cp -r build/* ../public/

# 3. Деплой
cd .. && kamal deploy
```

Или используйте GitHub Actions / GitLab CI для автоматизации.

## 🚀 После деплоя

- `http://your-domain.com/` → React фронтенд
- `http://your-domain.com/api/v1/products` → API
- `http://your-domain.com/admin` → Admin панель
- `http://your-domain.com/api-docs` → Swagger UI

Все работает через один контейнер и `kamal-proxy` на порту 80!

