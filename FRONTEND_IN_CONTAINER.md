# 🎨 Развертывание фронтенда в одном контейнере с Rails

## ✅ Варианты реализации

### Вариант 1: Статические файлы в `public/` (Рекомендуется)

**Как работает:**
- Фронтенд собирается (build) в статические файлы
- Файлы копируются в `public/` директорию Rails
- Rails обслуживает статику через `ActionDispatch::Static`
- Nginx на сервере проксирует все запросы к Rails

**Плюсы:**
- ✅ Простая реализация
- ✅ Один контейнер для всего
- ✅ Нет необходимости в отдельном Nginx в контейнере
- ✅ Rails автоматически обслуживает статику
- ✅ Простая маршрутизация (Rails routes)

**Минусы:**
- ⚠️ Нужно пересобирать образ при изменении фронтенда
- ⚠️ Увеличенный размер образа

**Реализация:**

1. **Сборка фронтенда:**
   ```bash
   # В директории фронтенда
   npm run build
   # или
   yarn build
   ```

2. **Копирование в Rails:**
   ```bash
   # Скопировать собранные файлы в public/
   cp -r frontend/dist/* rails_app/public/
   ```

3. **Обновить Dockerfile:**
   ```dockerfile
   # В Dockerfile добавить копирование фронтенда
   COPY frontend/dist /rails/public
   ```

4. **Настроить Rails routes:**
   ```ruby
   # config/routes.rb
   Rails.application.routes.draw do
     # API routes
     namespace :api do
       # ...
     end
     
     # Все остальные запросы - отдавать index.html (SPA)
     get '*path', to: 'frontend#index', constraints: ->(request) do
       !request.path.start_with?('/api') && 
       !request.path.start_with?('/admin') &&
       !request.path.start_with?('/api-docs')
     end
   end
   ```

5. **Создать контроллер для фронтенда:**
   ```ruby
   # app/controllers/frontend_controller.rb
   class FrontendController < ApplicationController
     def index
       render file: Rails.root.join('public', 'index.html'), layout: false
     end
   end
   ```

6. **Включить обслуживание статики в production:**
   ```ruby
   # config/environments/production.rb
   config.public_file_server.enabled = true
   ```

---

### Вариант 2: Nginx внутри контейнера

**Как работает:**
- Nginx установлен в контейнере
- Nginx обслуживает статику фронтенда
- Nginx проксирует API запросы к Rails (Puma)
- Оба сервиса запускаются через supervisor или systemd

**Плюсы:**
- ✅ Nginx лучше оптимизирован для статики
- ✅ Можно настроить кэширование
- ✅ Разделение ответственности

**Минусы:**
- ⚠️ Сложнее настройка
- ⚠️ Нужен процесс-менеджер (supervisor)
- ⚠️ Больше зависимостей в образе

**Реализация:**

1. **Установить Nginx в Dockerfile:**
   ```dockerfile
   RUN apt-get update -qq && \
       apt-get install --no-install-recommends -y \
       curl postgresql-client socat nginx && \
       rm -rf /var/lib/apt/lists /var/cache/apt/archives
   ```

2. **Конфигурация Nginx:**
   ```nginx
   # /etc/nginx/sites-available/default
   server {
       listen 80;
       
       # Статика фронтенда
       location / {
           root /rails/public;
           try_files $uri $uri/ /index.html;
       }
       
       # API
       location /api {
           proxy_pass http://127.0.0.1:3000;
           proxy_set_header Host $host;
       }
       
       # Admin
       location /admin {
           proxy_pass http://127.0.0.1:3000;
           proxy_set_header Host $host;
       }
   }
   ```

3. **Запуск через supervisor или скрипт:**
   ```bash
   # Запустить Nginx и Rails одновременно
   nginx && rails server
   ```

---

### Вариант 3: Гибридный подход (Текущая архитектура)

**Как работает:**
- Фронтенд на сервере (Nginx на хосте)
- Rails API в контейнере
- Nginx на хосте проксирует запросы

**Плюсы:**
- ✅ Разделение фронтенда и бэкенда
- ✅ Можно обновлять независимо
- ✅ Nginx на хосте может обслуживать несколько приложений

**Минусы:**
- ⚠️ Нужно настраивать Nginx на сервере
- ⚠️ Два места для деплоя

---

## 🎯 Рекомендация

**Для вашего случая рекомендую Вариант 1** (статичные файлы в `public/`):

1. ✅ Проще всего реализовать
2. ✅ Один контейнер для всего
3. ✅ Rails уже настроен для обслуживания статики
4. ✅ Не нужен дополнительный Nginx в контейнере
5. ✅ Простая маршрутизация через Rails routes

---

## 📝 Пошаговая реализация (Вариант 1)

### Шаг 1: Подготовка структуры

```bash
# В корне проекта
mkdir -p public
```

### Шаг 2: Обновить Dockerfile

```dockerfile
# Добавить после COPY . .
COPY frontend/dist /rails/public
```

### Шаг 3: Настроить Rails

```ruby
# config/routes.rb
Rails.application.routes.draw do
  # API, Admin, Swagger routes...
  
  # SPA fallback - должен быть последним
  get '*path', to: 'frontend#index', constraints: ->(request) do
    !request.path.start_with?('/api') && 
    !request.path.start_with?('/admin') &&
    !request.path.start_with?('/api-docs') &&
    !request.path.start_with?('/up')
  end
end
```

### Шаг 4: Создать контроллер

```ruby
# app/controllers/frontend_controller.rb
class FrontendController < ApplicationController
  def index
    render file: Rails.root.join('public', 'index.html'), layout: false
  end
end
```

### Шаг 5: Включить статику в production

```ruby
# config/environments/production.rb
config.public_file_server.enabled = true
```

### Шаг 6: Обновить Nginx на сервере

```nginx
# Убрать location / для фронтенда
# Все запросы идут к Rails
location / {
    proxy_pass http://ikea_api_backend;
    # ...
}
```

---

## 🔄 CI/CD процесс

```bash
# 1. Собрать фронтенд
cd frontend && npm run build

# 2. Скопировать в Rails
cp -r frontend/dist/* ../public/

# 3. Деплой через Kamal
kamal deploy
```

---

## ⚖️ Сравнение вариантов

| Критерий | Вариант 1 (public/) | Вариант 2 (Nginx) | Вариант 3 (Текущий) |
|----------|---------------------|-------------------|---------------------|
| Сложность | ⭐ Просто | ⭐⭐⭐ Сложно | ⭐⭐ Средне |
| Размер образа | Средний | Большой | Малый |
| Производительность | Хорошая | Отличная | Отличная |
| Независимость деплоя | Нет | Нет | Да |
| Простота настройки | ⭐⭐⭐ | ⭐ | ⭐⭐ |

---

## 💡 Рекомендация

**Используйте Вариант 1** - это самый простой и эффективный способ для вашего случая.

